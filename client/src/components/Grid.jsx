import React from 'react'

class Grid extends React.Component {

  render(){

    return(
      <div id ="grid">
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
        <button>{}</button>
      </div>
    )

  }

}

export default Grid